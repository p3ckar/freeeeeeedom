/*
All static variables list
 Merchant ID,
 server url,
 username,
 password,
 public key
  */
class config {
    static Merchant_id = "VRTPAY0001";
    static domain = "ISWKE";
    static  password = Math.random().toString(36).substring(2, 15) + Math.random().toString(36).substring(2, 15);
}

let vpApi_username = 'VARIOUS';

